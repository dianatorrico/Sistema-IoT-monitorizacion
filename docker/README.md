# Despliegue con Docker

Este directorio contiene la configuración necesaria para desplegar el sistema completo mediante Docker Compose.

Servicios:
- Mosquitto (broker MQTT)
- PostgreSQL
- Tomcat (backend Java)

## Ejecución

```bash
docker-compose up -d
